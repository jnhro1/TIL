## 1. .gitignore
- 깃에서 특정 파일 혹은 디렉토리를 관리 대상에서 제외할 때 사용하는 파일.
- 이 파일 안에 기입된 내용들은 모두 깃에서 관리하지 않겠다는 것을 의미한다. 
- 예를 들어 자동으로 생성되는 로그파일, 프로젝트 설정 파일 등을 관리 대상에서 제외할 수 있습니다. (db 계정 정보)  

### 실수로 gitignore 하기 전에 "git add ."를 했다면?
1. "git rm --cached 파일명 -r"
2. "git add ."
3. "git commit -m "message""
4. "git push origin master"
